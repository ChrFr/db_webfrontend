define(["backbone"],

    function(Backbone) {

        var RegionModel = Backbone.Model.extend({
            
            //url: 'db/gemeinden/{rs}',

            initialize: function(options) {
            },
        });
        return RegionModel;
    }
);